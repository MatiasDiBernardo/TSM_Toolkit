import matplotlib.pyplot as plt
import numpy as np

def compare_3_results(x1, x2, x3):
    """Plot three signal in a graph.

    Args:
        x1 (np.array): Original signal.
        x2 (np.array): Modified signal.
        x3 (np.array): Ideal signal
    """
    
    return None

